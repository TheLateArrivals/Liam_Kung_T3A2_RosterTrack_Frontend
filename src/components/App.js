import React from 'react'; // Imports the React library

// Defines a functional component called App
function App() {
  // Returns the JSX that should be rendered by this component
  return (//This is what will be displayed on the screen
    <div className="App">
      <h1>ROSTER TRACK</h1>
    </div>
  );
}

export default App; // Exports the App component so it can be imported in other files
